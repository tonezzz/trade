title: "Feature Template"
subtitle: "Replace this with a real feature name, e.g. ssot.ui.feature.data-source-selector.yml"
status: experimental
preview_page: compare2
promote_to: "ssot.ui.compare-family.yml"

# Description of what this feature changes/controls
description: |
  Brief description of the feature and which pages it affects.

# Ref to the base or family section this feature overrides
ref: "ssot.ui.yml#..."

# Override keys for the targeted section
# Example:
# strategy_panel:
#   title: "Strategy Comparison (with new feature)"
#   features:
#     new_feature: true

# Experimental toggles
toggles:
  enabled: false

# Notes for promotion
promotion_checklist:
  - Tested on compare2
  - No console errors
  - Added to ssot.index.yml
  - Synced to deployment
